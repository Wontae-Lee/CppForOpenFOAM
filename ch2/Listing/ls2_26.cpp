#include <string>
#include <iostream>
#include <vector>

using namespace std;

int main()
{

    vector<string> expression{"expression", "expression"};

    string ifInput;
    cin >> ifInput;

    if (ifInput == expression[0])
    {
        // code
    }
    else if (ifInput == expression[1])
    {
        // code
    }
    else
    {
        // code
    }

    return 0;
}
